## Video: Tracking Issues ##
cd health-checks/

code README.md
# this will open the contents of README.md file in VS Code editor:
## health-checks project
This repo will be populated with lots of fancy checks. 

Currently the main script is health_checks.py

This script will print "Everything ok" if all checks pass, 
or the corresponding error messages if some checks fail.

git commit -a
# this will prompt you to write the commit message (in your default editor):
Update README to use the new name of the script

Also add more information about how this works. 
Closes #1
(...)
# NOTE: "Closes #1" in the commit message will automatically close the issue on GitHub

git push
